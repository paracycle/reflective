source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in reflective.gemspec
gemspec

group "development" do
  gem "gel"
  gem "zeitwerk"
  gem "pry"
end
